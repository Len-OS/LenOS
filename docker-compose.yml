name: lenos

services:
  postgres:
    image: postgres:17-alpine
    container_name: lenos-postgres
    environment:
      POSTGRES_USER: lenos
      POSTGRES_PASSWORD: lenos_dev
      POSTGRES_DB: lenos
      PGDATA: /var/lib/postgresql/data
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - lenos-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lenos"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      com.lenos.service: "postgres"
      com.lenos.env: "dev"
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: lenos-redis
    ports:
      - "6379:6379"
    networks:
      - lenos-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    deploy:
      resources:
        limits:
          memory: 128m
    labels:
      com.lenos.service: "redis"
      com.lenos.env: "dev"
    restart: unless-stopped

  adminer:
    image: adminer:latest
    container_name: lenos-adminer
    ports:
      - "8082:8080"
    networks:
      - lenos-net
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    deploy:
      resources:
        limits:
          memory: 64m
    labels:
      com.lenos.service: "adminer"
      com.lenos.env: "dev"
    restart: unless-stopped

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    container_name: lenos-keycloak
    command: start-dev --http-port=8080
    environment:
      KC_DB: dev-mem
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    ports:
      - "8180:8080"
    networks:
      - lenos-net
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/8080 && echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && cat <&3 | grep -q '200 OK'"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      com.lenos.service: "keycloak"
      com.lenos.env: "dev"
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    container_name: lenos-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: lenos_dev
      MINIO_ROOT_PASSWORD: lenos_dev_secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    networks:
      - lenos-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 256m
    labels:
      com.lenos.service: "minio"
      com.lenos.env: "dev"
    restart: unless-stopped

  minio-init:
    image: minio/mc:latest
    container_name: lenos-minio-init
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - lenos-net
    entrypoint: >
      /bin/sh -c "
        mc alias set local http://minio:9000 lenos_dev lenos_dev_secret &&
        mc mb --ignore-existing local/lenos-media &&
        mc anonymous set none local/lenos-media
      "
    labels:
      com.lenos.service: "minio-init"
      com.lenos.env: "dev"
    restart: "no"

  prometheus:
    image: prom/prometheus:latest
    container_name: lenos-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - lenos-net
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 128m
    labels:
      com.lenos.service: "prometheus"
      com.lenos.env: "dev"
    restart: unless-stopped

volumes:
  postgres-data:
    name: lenos-postgres-data
    labels:
      com.lenos.volume: "postgres"
  minio-data:
    name: lenos-minio-data
    labels:
      com.lenos.volume: "minio"
  prometheus-data:
    name: lenos-prometheus-data
    labels:
      com.lenos.volume: "prometheus"

networks:
  lenos-net:
    name: lenos-net
    driver: bridge
    labels:
      com.lenos.network: "dev"

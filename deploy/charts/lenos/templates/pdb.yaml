{{- include "lenos.validate" . -}}
{{- if and .Values.podDisruptionBudget.enabled (gt (include "lenos.minimumReplicas" . | int) 1) -}}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "lenos.fullname" . }}
  labels:
    {{- include "lenos.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "lenos.relaySelectorLabels" . | nindent 6 }}
  {{- if .Values.podDisruptionBudget.minAvailable }}
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  {{- else if .Values.podDisruptionBudget.maxUnavailable }}
  maxUnavailable: {{ .Values.podDisruptionBudget.maxUnavailable }}
  {{- end }}
{{- end }}
